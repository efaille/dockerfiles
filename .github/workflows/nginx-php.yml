name: nginx-php ci

on:
  workflow_dispatch:
  workflow_call:
  push:
    branches: master
    paths: 'nginx-php/**'

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: efaille/actions/docker-base-image-ci@master
        with:
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
          context: nginx-php
          tag: efaille/nginx-php:latest
